<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>InTooDeep — homepage concept</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,500;0,9..144,600;1,9..144,400&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
<style>
  :root{
    --abyss:#091e28;
    --deep:#0f2c3a;
    --deep-2:#15394a;
    --deep-3:#1d4658;
    --surface:#5fc9c9;     /* light from above */
    --surface-soft:#8fdede;
    --coral:#ff7f63;       /* the lifeline / buoy */
    --coral-soft:#ff9d86;
    --foam:#eaf4f3;
    --foam-soft:#9fbdc0;
    --line:rgba(234,244,243,0.11);
    --line-strong:rgba(234,244,243,0.20);
    --display:"Fraunces", Georgia, "Times New Roman", serif;
    --body:"Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  }
  *{box-sizing:border-box;}
  html{scroll-behavior:smooth;}
  body{
    margin:0;
    background:var(--abyss);
    color:var(--foam);
    font-family:var(--body);
    line-height:1.6;
    -webkit-font-smoothing:antialiased;
  }
  a{color:inherit;text-decoration:none;}
  .wrap{max-width:1080px;margin:0 auto;padding:0 28px;}
  :focus-visible{outline:2px solid var(--coral);outline-offset:3px;border-radius:4px;}

  /* ---- nav ---- */
  .nav{display:flex;align-items:center;justify-content:space-between;padding:22px 0;border-bottom:1px solid var(--line);}
  .brand{display:flex;align-items:center;gap:11px;}
  .brand .mark{width:34px;height:34px;border-radius:50%;
    background:radial-gradient(circle at 34% 30%, var(--coral-soft), var(--coral) 72%);
    box-shadow:0 0 0 4px rgba(255,127,99,0.12);}
  .brand .name{font-family:var(--display);font-weight:500;font-size:20px;letter-spacing:.2px;}
  .brand .name b{font-weight:600;}
  .nav-links{display:flex;gap:26px;font-size:14px;color:var(--foam-soft);}
  .nav-links a:hover{color:var(--foam);}
  .nav-cta{font-size:14px;font-weight:600;color:var(--abyss);background:var(--coral);padding:9px 18px;border-radius:999px;}
  .nav-cta:hover{background:var(--coral-soft);}

  /* ---- hero ---- */
  .hero{position:relative;overflow:hidden;border-bottom:1px solid var(--line);
    background:linear-gradient(180deg, #0d2a37 0%, var(--abyss) 90%);}
  /* god-rays from the surface */
  .rays{position:absolute;inset:0;pointer-events:none;overflow:hidden;}
  .rays span{position:absolute;top:-40%;width:120px;height:170%;
    background:linear-gradient(180deg, rgba(95,201,201,0.16), rgba(95,201,201,0));
    transform:skewX(-14deg);filter:blur(6px);}
  .rays span:nth-child(1){left:8%;}
  .rays span:nth-child(2){left:30%;width:70px;opacity:.7;}
  .rays span:nth-child(3){left:62%;width:150px;opacity:.55;}
  .rays span:nth-child(4){left:82%;width:90px;opacity:.65;}
  .hero-inner{position:relative;padding:90px 0 80px;max-width:660px;}
  .eyebrow{font-size:12.5px;letter-spacing:2.6px;text-transform:uppercase;color:var(--surface);font-weight:600;margin-bottom:22px;}
  .hero h1{font-family:var(--display);font-weight:500;font-size:clamp(34px,5.3vw,58px);line-height:1.07;letter-spacing:-0.5px;margin:0 0 22px;}
  .hero h1 em{font-style:italic;color:var(--surface-soft);}
  .hero p{font-size:17px;color:var(--foam-soft);max-width:540px;margin:0 0 30px;}
  .subscribe{display:flex;gap:10px;max-width:450px;flex-wrap:wrap;}
  .subscribe input{flex:1;min-width:200px;background:rgba(234,244,243,0.05);border:1px solid var(--line-strong);color:var(--foam);
    padding:14px 16px;border-radius:10px;font-size:15px;font-family:var(--body);}
  .subscribe input::placeholder{color:var(--foam-soft);}
  .subscribe button{background:var(--coral);color:var(--abyss);border:none;font-weight:600;font-size:15px;
    padding:14px 22px;border-radius:10px;cursor:pointer;font-family:var(--body);}
  .subscribe button:hover{background:var(--coral-soft);}
  .sub-note{font-size:13px;color:var(--foam-soft);margin-top:13px;}

  /* ---- section scaffolding ---- */
  .block{padding:64px 0;border-bottom:1px solid var(--line);}
  .block-head{display:flex;align-items:flex-end;justify-content:space-between;gap:20px;margin-bottom:34px;}
  .block-head h2{font-family:var(--display);font-weight:500;font-size:30px;letter-spacing:-0.3px;margin:0;}
  .block-head .more{font-size:14px;color:var(--surface);font-weight:500;white-space:nowrap;}
  .block-head .more:hover{color:var(--surface-soft);}

  /* ---- SIGNATURE: the afloat / under ledger ---- */
  .ledger-title{font-family:var(--display);font-weight:500;font-size:30px;margin:0 0 4px;}
  .ledger-intro{font-size:15.5px;color:var(--foam-soft);max-width:580px;margin:0 0 34px;}
  .ledger{display:grid;grid-template-columns:1fr 1fr;gap:18px;}
  .col{background:var(--deep);border:1px solid var(--line);border-radius:16px;padding:26px 26px 14px;}
  .col-tag{display:inline-flex;align-items:center;gap:9px;font-size:12px;letter-spacing:1.5px;text-transform:uppercase;font-weight:600;margin-bottom:18px;}
  .col-tag .pip{width:9px;height:9px;border-radius:50%;}
  .afloat .col-tag{color:var(--coral-soft);}
  .afloat .pip{background:var(--coral);}
  .under .col-tag{color:var(--surface);}
  .under .pip{background:var(--surface);}
  .item{display:flex;gap:13px;padding:13px 0;border-top:1px solid var(--line);font-size:15px;align-items:baseline;}
  .col > .item:first-of-type{border-top:none;}
  .item .glyph{flex:none;width:18px;font-family:var(--display);}
  .afloat .glyph{color:var(--coral);}
  .under .glyph{color:var(--surface);}
  .under .item span.txt{color:var(--foam-soft);text-decoration:line-through;text-decoration-color:rgba(143,222,222,0.5);}
  .item small{display:block;color:var(--foam-soft);font-size:13px;text-decoration:none;margin-top:2px;}

  /* ---- stories ---- */
  .grid{display:grid;grid-template-columns:repeat(3,1fr);gap:20px;}
  .story{background:var(--deep);border:1px solid var(--line);border-radius:16px;overflow:hidden;display:flex;flex-direction:column;transition:border-color .2s;}
  .story:hover{border-color:var(--line-strong);}
  .story .img{height:140px;}
  .story .img.one{background:linear-gradient(150deg,#14384a,#5fc9c9);}
  .story .img.two{background:linear-gradient(150deg,#173f3a,#ff7f63);}
  .story .img.three{background:linear-gradient(150deg,#1c3550,#8fdede);}
  .story .pad{padding:20px 20px 22px;display:flex;flex-direction:column;flex:1;}
  .chip{align-self:flex-start;font-size:11px;letter-spacing:1.2px;text-transform:uppercase;font-weight:600;
    color:var(--surface-soft);background:rgba(95,201,201,0.12);padding:5px 11px;border-radius:999px;margin-bottom:14px;}
  .story h3{font-family:var(--display);font-weight:500;font-size:19px;line-height:1.28;margin:0 0 10px;}
  .story p{font-size:14px;color:var(--foam-soft);margin:0 0 18px;flex:1;}
  .story .meta{font-size:12.5px;color:var(--foam-soft);display:flex;gap:9px;align-items:center;}
  .story .meta .d{width:3px;height:3px;border-radius:50%;background:var(--foam-soft);}

  /* ---- community band ---- */
  .band{position:relative;overflow:hidden;background:linear-gradient(135deg,var(--deep-3),var(--deep));border:1px solid var(--line-strong);
    border-radius:20px;padding:42px 44px;display:flex;align-items:center;justify-content:space-between;gap:28px;flex-wrap:wrap;}
  .band h2{font-family:var(--display);font-weight:500;font-size:28px;margin:0 0 8px;}
  .band p{font-size:15px;color:var(--foam-soft);margin:0;max-width:470px;}
  .band .btn{background:var(--coral);color:var(--abyss);font-weight:600;font-size:15px;padding:14px 26px;border-radius:999px;white-space:nowrap;}
  .band .btn:hover{background:var(--coral-soft);}

  /* ---- categories ---- */
  .cats{display:grid;grid-template-columns:repeat(4,1fr);gap:16px;}
  .cat{background:var(--deep);border:1px solid var(--line);border-radius:14px;padding:22px 20px;}
  .cat .ic{font-size:22px;margin-bottom:12px;display:block;}
  .cat h4{font-family:var(--display);font-weight:500;font-size:17px;margin:0 0 6px;}
  .cat p{font-size:13px;color:var(--foam-soft);margin:0;}

  /* ---- footer ---- */
  footer{padding:54px 0 40px;}
  .foot-top{display:flex;justify-content:space-between;gap:30px;flex-wrap:wrap;align-items:flex-start;margin-bottom:34px;}
  .foot-sign{max-width:360px;}
  .foot-sign h3{font-family:var(--display);font-weight:500;font-size:22px;margin:0 0 10px;}
  .foot-sign p{font-size:14px;color:var(--foam-soft);margin:0 0 16px;}
  .foot-cols{display:flex;gap:54px;flex-wrap:wrap;}
  .foot-cols h5{font-size:12px;letter-spacing:1.4px;text-transform:uppercase;color:var(--foam-soft);margin:0 0 14px;font-weight:600;}
  .foot-cols a{display:block;font-size:14px;color:var(--foam);margin-bottom:10px;}
  .foot-cols a:hover{color:var(--coral-soft);}
  .foot-base{border-top:1px solid var(--line);padding-top:22px;font-size:13px;color:var(--foam-soft);display:flex;justify-content:space-between;flex-wrap:wrap;gap:10px;}

  @media (max-width:860px){
    .grid{grid-template-columns:1fr 1fr;}
    .cats{grid-template-columns:1fr 1fr;}
  }
  @media (max-width:640px){
    .nav-links{display:none;}
    .ledger{grid-template-columns:1fr;}
    .grid{grid-template-columns:1fr;}
    .cats{grid-template-columns:1fr;}
    .hero-inner{padding:64px 0 56px;}
    .band{padding:32px 28px;}
  }
  @media (prefers-reduced-motion:reduce){
    html{scroll-behavior:auto;}
    *{transition:none !important;}
  }
</style>
</head>
<body>

  <!-- NAV -->
  <div class="wrap">
    <nav class="nav">
      <div class="brand">
        <div class="mark" aria-hidden="true"></div>
        <div class="name">In<b>Too</b>Deep</div>
      </div>
      <div class="nav-links">
        <a href="#">Stories</a>
        <a href="#">Afloat &amp; under</a>
        <a href="#">Resources</a>
        <a href="#">Community</a>
        <a href="#">About</a>
      </div>
      <a href="#join" class="nav-cta">Join free</a>
    </nav>
  </div>

  <!-- HERO -->
  <header class="hero">
    <div class="rays" aria-hidden="true"><span></span><span></span><span></span><span></span></div>
    <div class="wrap">
      <div class="hero-inner">
        <div class="eyebrow">Single motherhood · no filter</div>
        <h1>Some days you're <em>in too deep.</em> You were never meant to swim it alone.</h1>
        <p>Honest stories, hard-won lessons, and a community of single moms in the deep end together — what keeps us afloat, what doesn't, and everything nobody warns you about.</p>
        <form class="subscribe" id="join" onsubmit="return false;">
          <input type="email" placeholder="Your email" aria-label="Your email" />
          <button type="submit">Send me the letter</button>
        </form>
        <div class="sub-note">A letter every Sunday morning. Free, and yours to leave anytime.</div>
      </div>
    </div>
  </header>

  <!-- SIGNATURE: AFLOAT / UNDER LEDGER -->
  <section class="block">
    <div class="wrap">
      <h2 class="ledger-title">What keeps me afloat. What was pulling me under.</h2>
      <p class="ledger-intro">The running ledger of single-mom life — the things that actually hold me up, and the ones I finally stopped letting drag me down. Updated as I learn.</p>
      <div class="ledger">
        <div class="col afloat">
          <div class="col-tag"><span class="pip"></span> Keeps me afloat</div>
          <div class="item"><span class="glyph">✓</span><span class="txt">Sunday "reset" hour after bedtime<small>Lay out the week before it can ambush me.</small></span></div>
          <div class="item"><span class="glyph">✓</span><span class="txt">One freezer meal on standby, always<small>Saves the nights I have nothing left to give.</small></span></div>
          <div class="item"><span class="glyph">✓</span><span class="txt">Saying yes when someone offers help<small>Pride doesn't fold laundry.</small></span></div>
          <div class="item"><span class="glyph">✓</span><span class="txt">The 11pm "are you up?" text thread<small>Two other single moms who just get it.</small></span></div>
        </div>
        <div class="col under">
          <div class="col-tag"><span class="pip"></span> Was pulling me under</div>
          <div class="item"><span class="glyph">✕</span><span class="txt">Pinterest-perfect birthday parties<small>They never remembered the centerpiece anyway.</small></span></div>
          <div class="item"><span class="glyph">✕</span><span class="txt">Pretending I'm fine for everyone else<small>It cost more than it saved.</small></span></div>
          <div class="item"><span class="glyph">✕</span><span class="txt">The color-coded everything<small>One shared list beats five perfect systems.</small></span></div>
          <div class="item"><span class="glyph">✕</span><span class="txt">Guilt about screen time on hard days<small>Survival counts as good parenting.</small></span></div>
        </div>
      </div>
    </div>
  </section>

  <!-- STORIES -->
  <section class="block">
    <div class="wrap">
      <div class="block-head">
        <h2>Latest stories</h2>
        <a class="more" href="#">All stories →</a>
      </div>
      <div class="grid">
        <article class="story">
          <div class="img one"></div>
          <div class="pad">
            <span class="chip">The deep end</span>
            <h3>The night I cried in the car so they wouldn't see</h3>
            <p>On letting yourself go under for a minute — and why hiding it isn't always the kindness we think it is.</p>
            <div class="meta">8 min read <span class="d"></span> 142 comments</div>
          </div>
        </article>
        <article class="story">
          <div class="img two"></div>
          <div class="pad">
            <span class="chip">Keeps me afloat</span>
            <h3>My $40 meal system that survives actual chaos</h3>
            <p>The grocery routine that finally stuck. Built for exhaustion, not for the highlight reel.</p>
            <div class="meta">6 min read <span class="d"></span> 89 comments</div>
          </div>
        </article>
        <article class="story">
          <div class="img three"></div>
          <div class="pad">
            <span class="chip">Co-parenting</span>
            <h3>The boundary that finally made handoffs bearable</h3>
            <p>One script, one rule, and the end of the driveway arguments my kids didn't need to hear.</p>
            <div class="meta">5 min read <span class="d"></span> 67 comments</div>
          </div>
        </article>
      </div>
    </div>
  </section>

  <!-- COMMUNITY BAND -->
  <section class="block">
    <div class="wrap">
      <div class="band">
        <div>
          <h2>Real Talk Tuesdays</h2>
          <p>Every week, one reader shares her story — anonymously if she wants — and the rest of us show up in the comments. Come read, or throw out your own line.</p>
        </div>
        <a class="btn" href="#">Share your story</a>
      </div>
    </div>
  </section>

  <!-- CATEGORIES -->
  <section class="block">
    <div class="wrap">
      <div class="block-head">
        <h2>Find what you need tonight</h2>
        <a class="more" href="#">Browse all →</a>
      </div>
      <div class="cats">
        <a class="cat" href="#"><span class="ic">🕯️</span><h4>Grief &amp; healing</h4><p>Loss, divorce, the life you didn't plan for.</p></a>
        <a class="cat" href="#"><span class="ic">💸</span><h4>Money &amp; benefits</h4><p>One income, real budgets, where to get help.</p></a>
        <a class="cat" href="#"><span class="ic">🧩</span><h4>Co-parenting</h4><p>Boundaries, custody, and keeping your peace.</p></a>
        <a class="cat" href="#"><span class="ic">🌅</span><h4>Coming up for air</h4><p>The reminder that you're a person too.</p></a>
      </div>
    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    <div class="wrap">
      <div class="foot-top">
        <div class="foot-sign">
          <h3>One honest letter, every Sunday.</h3>
          <p>No spam, no selling, no toxic positivity. Just the company of someone treading the same water.</p>
          <form class="subscribe" onsubmit="return false;">
            <input type="email" placeholder="Your email" aria-label="Your email" />
            <button type="submit">Subscribe</button>
          </form>
        </div>
        <div class="foot-cols">
          <div>
            <h5>Read</h5>
            <a href="#">Latest stories</a>
            <a href="#">Afloat &amp; under</a>
            <a href="#">Grief &amp; healing</a>
          </div>
          <div>
            <h5>Community</h5>
            <a href="#">Share your story</a>
            <a href="#">The group chat</a>
            <a href="#">Resources</a>
          </div>
          <div>
            <h5>About</h5>
            <a href="#">Her story</a>
            <a href="#">Contact</a>
            <a href="#">Privacy</a>
          </div>
        </div>
      </div>
      <div class="foot-base">
        <span>InTooDeep · a community for single moms</span>
        <span>© 2026 · written from the deep end</span>
      </div>
    </div>
  </footer>

</body>
</html>
